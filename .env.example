# ============================================================================
# Environment Variables for ComfyUI MCP Server
# ============================================================================
# Priority: Environment Variables > config.json > Default Values
# ============================================================================

# ----------------------------------------------------------------------------
# ComfyUI Configuration
# ----------------------------------------------------------------------------

# ComfyUI server address (overrides config.json)
# Default: http://127.0.0.1:8188
COMFYUI_ADDRESS=http://127.0.0.1:8188

# ComfyUI client identifier
# Default: comfyui-mcp-client
COMFYUI_CLIENT_ID=comfyui-mcp-client

# ----------------------------------------------------------------------------
# AWS S3 Configuration (Optional - for image storage)
# ----------------------------------------------------------------------------

# Enable S3 upload for generated images
# Values: true, false, 1, 0
# Default: false
S3_ENABLE=false

# AWS Credentials (supports both AWS_ and S3_ prefixes)
# S3_ prefix takes priority over AWS_ prefix
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Alternatively (higher priority):
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=

# S3 Bucket Configuration
# Default: (from config.json or empty)
S3_BUCKET=your-s3-bucket-name
S3_REGION=us-east-1

# S3-compatible endpoint (optional, for MinIO, DigitalOcean Spaces, etc.)
# Default: (uses AWS S3 endpoint)
S3_ENDPOINT=

# Custom public domain for S3 URLs (optional)
# Default: https://s3.{region}.amazonaws.com
S3_PUBLIC_DOMAIN=

# Enable path-style addressing (S3ForcePathStyle)
# Use true for MinIO or other S3-compatible services
# Values: true, false, 1, 0
# Default: false
S3_ENABLE_PATH_STYLE=false

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------

# Server port (for FastMCP HTTP streaming transport)
# Default: 8080
PORT=8080

# Express server port (for Swagger UI documentation)
# Default: 3000
EXPRESS_PORT=3000

# ----------------------------------------------------------------------------
# Development
# ----------------------------------------------------------------------------

# Log level
# Values: error, warn, info, debug
# Default: info
LOG_LEVEL=info

# Log directory (for file-based logging)
# Default: ./logs
LOG_DIR=./logs

# Environment (affects console logging behavior)
# Values: development, production
# Default: development
# Note: Console logging is enabled in non-production environments
NODE_ENV=development

# ----------------------------------------------------------------------------
# HTTP Client Configuration (for outbound requests to ComfyUI, S3, etc.)
# ----------------------------------------------------------------------------

# Proxy configuration for HTTP/HTTPS requests
# Useful when accessing remote ComfyUI servers through a corporate proxy
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# NO_PROXY - comma-separated list of hosts that should bypass the proxy
# Default: (none)
# NO_PROXY=localhost,127.0.0.1,.local

# SSL/TLS certificate verification control
# WARNING: Only disable SSL verification for development or with self-signed certificates!
# Values: true, false
# Default: false
# DISABLE_SSL_VERIFY=false

# Alternative: Set NODE_TLS_REJECT_UNAUTHORIZED=0 to disable SSL verification
# This is a standard Node.js environment variable
# NODE_TLS_REJECT_UNAUTHORIZED=0

# Enable HTTP request debugging
# Values: true, false
# Default: false
# DEBUG_HTTP=false
